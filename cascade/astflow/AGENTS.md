# cascade/astflow/ — AST dataflow extraction

Reads source into a dataflow IR for tracer evidence and spec verification. Like the
linters, it never imports the code it analyses — parse trees only. House rules:
../../AGENTS.md.

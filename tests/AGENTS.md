# tests/ — the behavioral spec

These tests are artifact 3 of ../PORTING.md: the executable statement of what any
conforming implementation must do, and the conformance gate for ports. A behavior
change without a test change is suspect. Tests describe behavior, never history or
tickets. Import via `cascade.*`; run from the repo root.

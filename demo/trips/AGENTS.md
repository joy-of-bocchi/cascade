# demo/trips/ — trips worked example

Generic trips and legs demonstration for the quickstart-adjacent generated
artifacts. Keep the domain generic, deterministic, and self-contained. Generated
files in `out/` are committed drift gates; regenerate them with
`python -m demo.trips.generate`.
